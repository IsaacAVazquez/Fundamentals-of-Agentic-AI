---
version: 1
slug: "components-contacts-page-tsx"
primary_target: "components/contacts-page.tsx"
related_targets: ["app/page.tsx","app/sign-in/page.tsx","app/sign-up/page.tsx","components/auth-form.tsx","components/contact-form.tsx","app/layout.tsx"]
---

# Surface brief: the whole app (contacts list, sign in, sign up, dialogs)

Mode: operate. Grain: product. Seed key 9bae0f3a, direction "The Class Directory", locked on 2026-09-08, code-led.

Audience and job. Me on a phone right after a Haas event writing down who I met, and me on a laptop scanning the list to pick who to follow up with. Second audience is a grader or hiring manager arriving cold with a test account, judging in under a minute whether it reads like a real product. Privacy is the product truth; nothing implies sharing.

Decisions not to re-litigate. World is a bound Berkeley class directory: Berkeley Blue cloth cover with gold stamping, bright white uncoated listing stock (never cream), fore-edge thumb index, hanging-indent listing type. Color strategy is committed: blue owns the chrome regions only (top band, thumb index), gold is reserved for the active tab and the high register, the listing is black on white. Sign in and sign up are the cover with the form as a bookplate. The priority filter is the thumb index. Four binding raises: every priority state has its own typographic register; one hanging-indent axis rules entries and running head on both breakpoints; running-head counts change visibly on filter; band, running head, and thumb index never move while sort and filter swap only the listing. Type: Libre Franklin for listing and controls, Archivo Narrow for running heads and tabs.

Anti-goals. Dark theme, sidebar shell, social or sharing cues, a literal network graph, cream paper, new fields or features.

Untouched. Feature set, factual copy, validation, auth flow, ARIA roles, native selects, README.

Verified state. Built 2026-09-08, inspected at 1440, 1280, and 390, finish review disposition ship with all seven fixes scored resolved, DESIGN.md recorded the same day. Captures in .impeccable/review; the dialog and empty-state captures predate the fix batch.

Commands worth running here. critique and audit after the build; polish last. Never bolder and quieter on the same pass.
